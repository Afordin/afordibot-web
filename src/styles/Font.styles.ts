import { css } from 'styled-components'

export module FontStyles {
	export const RootOfLife = css`
    font-family: 'Root of Life', sans-serif;
  `

	export const Raleway = css`
    font-family: 'Raleway', sans-serif;
  `
}
